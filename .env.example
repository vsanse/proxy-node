# ============================================
# PROXYKIT CONFIGURATION
# ============================================

# Server port (default: 3001)
PORT=3001

# ============================================
# OPTION 1: Single Target (Simple Setup)
# ============================================

# Target API URL (required for single target mode)
TARGET_API=https://your-staging-api.com

# Cookie string to attach to all requests (optional - can use headers only)
COOKIE_STRING=

# Custom headers (optional) - JSON format
# Example: {"Authorization": "Bearer token123", "X-Custom-Header": "value"}
CUSTOM_HEADERS=

# ============================================
# OPTION 2: Multiple Targets (Advanced Setup)
# ============================================
# Uncomment and configure TARGETS to use multi-target routing
# When TARGETS is set, TARGET_API/COOKIE_STRING/CUSTOM_HEADERS are ignored

# TARGETS=[{"name":"api","pattern":"/api/*","target":"https://api.example.com","cookies":"session=abc","headers":{"Authorization":"Bearer token"}},{"name":"default","pattern":"/*","target":"https://example.com","cookies":"","headers":{}}]

# ============================================
# LOGGING CONFIGURATION
# ============================================

# Enable/disable logging (default: true)
LOG_ENABLED=true

# Write logs to files (default: false)
LOG_TO_FILE=false

# Log directory (default: ./logs)
LOG_DIR=./logs

# Log request bodies - useful for debugging POST/PUT requests (default: false)
LOG_REQUEST_BODY=false

# Log response bodies - useful for debugging API responses (default: false)
LOG_RESPONSE_BODY=false
